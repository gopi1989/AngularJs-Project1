'use strict';

module.exports = function (additionalFiles, src, type) {
    additionalFiles.forEach(function (addFile) {
        if (addFile.path.endsWith('.' + type)) {
            src.push(addFile);
        }
    });
};
